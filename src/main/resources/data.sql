-- The seed data has been removed to prevent duplicate entries from being created
-- every time the application starts.
-- Since the database is now permanent, you can add data via the application UI.

SELECT 1;
